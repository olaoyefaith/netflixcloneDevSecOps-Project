apiVersion: v1
kind: Service
metadata:
  name: netflix-app
  labels:
    app: netflix-app
  namespace: default  
spec:
  type: LoadBalancer
  ports:
  - port: 8080
    targetPort: 80
  selector:
    app: netflix-app
